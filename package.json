{
  "name": "ideosuite-gallery",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "npx mix --production; npm run generate:html",
    "serve": "browser-sync start --server docs --files \"src/\"",
    "dev": "npm run watch",
    "watch": "npx mix watch",
    "generate:html": "node publish.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "browser-sync-webpack-plugin": "^2.3.0",
    "graceful-fs": "^4.2.11",
    "laravel-mix": "^6.0.49",
    "normalize.css": "^8.0.1"
  },
  "dependencies": {
    "masonry-layout": "^4.2.2"
  }
}
