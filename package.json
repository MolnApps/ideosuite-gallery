{
  "name": "ideosuite-gallery",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "browser-sync start --server public --files \"src/\"",
    "watch": "npx mix watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "laravel-mix": "^6.0.49",
    "normalize.css": "^8.0.1"
  }
}
